# Docker profile configuration
# Database connection is handled by environment variables in .env.docker

logging:
  level:
    root: INFO
    py.gov.mspbs.javacunas: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
