meta {
  name: Unauthorized Access - No Token
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/vaccines
  body: none
  auth: none
}

tests {
  test("should return 401 or 403 status (unauthorized)", function() {
    const status = res.getStatus();
    expect([401, 403]).to.include(status);
  });

  test("should return error response", function() {
    const status = res.getStatus();
    // Only check for error message if we get an error status
    expect([401, 403]).to.include(status);
  });
}
