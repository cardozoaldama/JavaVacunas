meta {
  name: Delete Child - Doctor Only
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/children/{{testChildId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{doctorToken}}
}

tests {
  test("should return 204 status", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
