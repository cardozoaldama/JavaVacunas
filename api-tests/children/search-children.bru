meta {
  name: Search Children by Name
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/children/search?query=Juan
  body: none
  auth: bearer
}

auth:bearer {
  token: {{doctorToken}}
}

tests {
  test("should return 200 status", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("should return array of matching children", function() {
    const data = res.getBody();
    expect(data).to.be.an("array");
  });
}
