meta {
  name: Create Vaccination - Forbidden for Parent
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/vaccinations
  body: json
  auth: bearer
}

auth:bearer {
  token: {{parentToken}}
}

body:json {
  {
    "childId": {{testChildId}},
    "vaccineId": {{testVaccineId}},
    "doseNumber": 1,
    "administrationDate": "2024-12-01",
    "batchNumber": "TEST-001",
    "expirationDate": "2025-12-01",
    "administrationSite": "Brazo derecho"
  }
}

tests {
  test("should return 403 status", function() {
    expect(res.getStatus()).to.equal(403);
  });
}
