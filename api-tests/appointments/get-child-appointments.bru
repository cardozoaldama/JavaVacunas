meta {
  name: Get Appointments for Child
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/appointments/child/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{parentToken}}
}

tests {
  test("should return 200 status", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("should return array of appointments", function() {
    const data = res.getBody();
    expect(data).to.be.an("array");
  });
}
